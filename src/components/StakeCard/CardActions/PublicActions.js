/**
 * Defines the actions someone can take if
 * they don't have a relationship with the stake.
 */

const PublicActions = [
    {
        name: 'learnMore',
        label: 'Learn More About This Staker',
        onSubmit: console.log
    }
]

export default PublicActions
